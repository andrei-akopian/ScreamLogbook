# Codex for remote control

`codex remote-contol start`

`codex remote-contol pair`

put the code into settings > connections > control other devices.

In the app: new project > remote > select relevant remote.
